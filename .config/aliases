# Manage dotfiles with bare git repo
alias gitd="git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME"
alias gits="git --git-dir=$HOME/.sysportage --work-tree=/etc/portage"
alias cdp="cd /etc/portage"

# Clean dwm build
alias dwmresetbuild="sudo make clean && rm -f config.h && git reset --hard upstream/master"

# Suckless reinstall
alias srein="sudo rm -f config.h && sudo make clean install"

# Exits dwm
alias dwmq="killall startdwm.sh"

# Colors
alias \
	ls="ls --color=auto --group-directories-first" \
	grep="grep --color=auto" \
	egrep="egrep --color=auto" \
	fgrep="fgrep --color=auto" \
	diff="diff --color=auto"

alias \
	cp="cp -iv" \
	mv="mv -iv" \
	rm="rm -Iv" \
	mkdir="mkdir -pv" \
